anthropic==0.40.0
requests==2.32.3
PyYAML==6.0.2
python-frontmatter==1.1.0
pydantic==2.9.2

# Scientific Python — needed by the demo subprocess (run_demo.py executes
# whatever code Claude generates in the `demo_code` stage). The stream YAML
# tells Claude it can use numpy / matplotlib / scikit-learn / scipy. Pytorch
# is intentionally omitted (huge install) — if a stream needs it later,
# add it here and bump the workflow timeout.
numpy==2.1.3
matplotlib==3.9.3
scikit-learn==1.5.2
scipy==1.14.1
pillow==11.0.0

# Cover image generation (C6 — DALL-E 3 via OpenAI API). Lightweight, only
# pulled in when the cover_image stage runs.
openai==1.55.0
