anthropic==0.40.0
requests==2.32.3
PyYAML==6.0.2
python-frontmatter==1.1.0
pydantic==2.9.2

# Scientific Python — needed by the demo subprocess (run_demo.py executes
# whatever code Claude generates in the `demo_code` stage). The stream YAML
# tells Claude it can use numpy / matplotlib / scikit-learn / scipy / seaborn.
# Pytorch is intentionally omitted (huge install) — if a stream needs it
# later, add it here and bump the workflow timeout.
#
# Seaborn is optional from the style pack's perspective (plot_style.py
# imports it under a try/except) but it's small (~6 MB) and gives Claude
# more polished options for distributions, heatmaps, and pair plots, so
# we include it unconditionally to keep the editorial style consistent.
numpy==2.1.3
matplotlib==3.9.3
scikit-learn==1.5.2
scipy==1.14.1
seaborn==0.13.2
pillow==11.0.0

# Cover image generation (C6 — DALL-E 3 via OpenAI API). Lightweight, only
# pulled in when the cover_image stage runs.
#
# NOTE on the version: openai 1.55.0 broke when httpx>=0.28 dropped its
# `proxies` argument — the SDK was passing it through unconditionally and
# the runner crashed at OpenAI() construction. Fixed in openai>=1.55.3.
# Pinning to 1.59.6 (1.59.0 / 1.59.1 were never published to PyPI; the
# 1.59.x series begins at 1.59.2 and .6 is a mid-series patch well past
# the proxies fix). httpx is range-bounded as a defensive
# belt-and-suspenders so a future SDK update can't surprise us.
openai==1.59.6
httpx>=0.27,<0.29
